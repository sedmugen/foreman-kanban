# Core Dependencies
fastapi>=0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.3

# Database & Mocks
# NOTE: 'bson' is included in 'pymongo'. Do NOT add standalone 'bson' package to requirements.txt, as it breaks PyMongo.
pymongo>=4.9,<4.10
motor==3.6.0

# External Integrations & Configuration
python-dotenv==1.0.1
firebase-admin==6.6.0
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# IDE Troubleshooting:
# If your IDE displays warnings/errors for 'fastapi', 'pydantic', or 'bson', ensure that your 
# IDE's Python interpreter is set to the virtual environment path:
# ./backend/venv/bin/python
starlette>=1.3.1
